# Lessons & Gotchas: [Module Name]

Whenever a bug is fixed, a tricky UI issue is resolved, or a specific architectural decision is made, document it here so future AI agents do not repeat the mistake.

## 📝 General Rules
- Rule 1: ...

## 🐛 Past Bugs & Fixes
- **[Date] - [Bug Description]**:
  - *Cause*: Why did it happen?
  - *Fix*: How was it fixed? (e.g., "Do not use `flex-col` on this wrapper because it breaks the embla carousel. Use `grid` instead.")
